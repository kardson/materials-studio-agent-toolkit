use strict;
use warnings;
use MaterialsScript qw(:all);

my $doc = Documents->Item("{{ input_xsd }}");
my $results = Modules->CASTEP->Energy->Run($doc, Settings(
  Quality => "{{ quality|default('Coarse') }}"
));
my $result_doc = $results->Structure;
print "submitted\n";
